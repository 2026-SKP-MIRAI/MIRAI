# services/siw/ — 성시우

## 목적
MVP 01 완료 — 자소서 업로드 → 질문 생성 end-to-end.
DDD (Domain-Driven Design) + TDD 적용.

## 구조
```
siw/
├── src/
│   ├── app/
│   │   ├── layout.tsx                    # RootLayout (lang="ko")
│   │   ├── resume/page.tsx               # 업로드+결과 단일 페이지
│   │   └── api/resume/questions/route.ts # POST → 엔진 프록시
│   ├── components/
│   │   ├── UploadForm.tsx                # 상태머신 (idle→ready→uploading→done/error)
│   │   └── QuestionList.tsx             # 카테고리별 질문 + 다시하기
│   └── lib/
│       ├── types.ts                      # Category, QuestionItem, QuestionsResponse, UploadState
│       └── error-messages.ts             # ENGINE_ERROR_MESSAGES + mapDetailToKey
└── tests/
    ├── setup.ts
    ├── api/
    │   ├── error-messages.test.ts        # 7개 (node 환경)
    │   └── resume-questions-route.test.ts # 6개 (node 환경)
    └── ui/
        ├── upload-form.test.tsx           # 5개 (jsdom 환경)
        └── question-results.test.tsx      # 2개 (jsdom 환경)
```

## 역할
- 기능 흐름: 자소서 업로드 → 질문 생성 → (Week 2~) 면접 시뮬레이션 → 역량 평가
- engine/ 만 호출 — LLM·파서 직접 구현 금지
- ENGINE_BASE_URL 환경변수로 엔진 접근 (기본값: http://localhost:8000)
- Week 1: MVP 완료 (자소서 → 질문 생성 end-to-end 작동)
- Week 2~3: 기획서 기능 2~4 (꼬리질문, 페르소나, 역량 평가) 추가
- Week 4: 특색 기능 1개 이상 + 배포 URL 제출

## 기술 스택
- Next.js 15, React 19, TypeScript (strict)
- Vitest 2, @testing-library/react 16
- 테스트: 총 20개 (API 13개 node 환경 + UI 7개 jsdom 환경)

## 인증·DB·스토리지
- **인증**: Supabase Auth (Better Auth 미사용)
  - 클라이언트: `@supabase/ssr` createBrowserClient + `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - 서버: createServerClient + `middleware.ts` (`getUser()`로 세션 갱신 필수)
- **DB**: Supabase PostgreSQL
  - 런타임: `DATABASE_URL` (pooler, port 6543, `?pgbouncer=true`)
  - 마이그레이션: `DIRECT_URL` (direct, port 5432)
  - Prisma로 테이블 생성 시 RLS 자동 활성화 안됨 — 별도 SQL 마이그레이션 필수
- **파일 저장**: Supabase Storage (AWS S3 미사용)
  - 버킷명: `SUPABASE_STORAGE_BUCKET` 환경변수 사용 (하드코딩 금지)
  - 서버 경유 업로드 (WAF 보호 유지)
- **보안**: `SUPABASE_SERVICE_ROLE_KEY`는 서버 전용 — `NEXT_PUBLIC_` 접두사 절대 금지
- 환경변수 상세: `docs/specs/mirai/dev_spec.md` §6

## 작업 전 확인
- `engine/.ai.md` — 엔진 API 계약 확인
- `docs/specs/` — 기능 AC 확인
- 이 파일 (.ai.md) — 현재 구조·진행 상태 확인

## 진행 상태
- [x] Week 1 MVP 01: 자소서 업로드 → 질문 생성 end-to-end (완료)
- [ ] Week 2: 꼬리질문, 페르소나 기능
- [ ] Week 3: 역량 평가
- [ ] Week 4: 특색 기능 + 배포

## 규칙 변경 시 컨펌 필요
이 파일에 기재된 규칙과 충돌하는 변경을 하려는 경우, 작업을 진행하기 전에 반드시 사람(팀원)에게 먼저 확인을 받아야 한다.
