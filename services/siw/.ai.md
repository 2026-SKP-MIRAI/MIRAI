# services/siw/ — 성시우

## 목적
MVP 01 완료 — 자소서 업로드 → 질문 생성 end-to-end.
Phase 1 완료 — 면접 시뮬레이션 (패널 면접 세션 + 꼬리질문).
Week 3 완료 — 8축 역량 평가 리포트 (면접 완료 → 엔진 호출 → 리포트 렌더링).
Issue #87 완료 — 전체 내비게이션 재설계 (4탭 Sidebar + 3개 신규 페이지 + 성장 추이 API + 면접 종료 기능).
Issue #89 완료 — 이력서 영속 저장 (Resume Prisma 모델 + Supabase Storage 업로드 + per-user 조회).
Issue #117 완료 — Docker 컨테이너화 + ECR/EC2 CI/CD 파이프라인 구축.
Issue #133 완료 — Docker 배포 안정화 (Prisma symlink 수정, 스테이지 빌드 강화, HEALTHCHECK 추가).
DDD (Domain-Driven Design) + TDD 적용.

## 구조
```
siw/
├── Dockerfile                        # Next.js standalone 멀티스테이지 빌드 (node:20-alpine, non-root, Prisma 포함, HEALTHCHECK)
├── entrypoint.sh                     # 컨테이너 시작 스크립트 (env guard → prisma migrate deploy via node prisma/build/index.js → node server.js)
├── .dockerignore                     # Docker 빌드 컨텍스트 제외 목록
├── README.md                         # 로컬 Docker 빌드/실행/환경변수/네트워크/Secrets 가이드
├── prisma/
│   ├── schema.prisma                 # Resume 모델 + InterviewSession 모델 (resumeId 참조 추가)
│   └── migrations/
│       ├── 20260313_add_report_scores/migration.sql  # reportScores, reportTotalScore 컬럼 추가
│       ├── 20260315000002_add_resume_model/migration.sql  # Resume 테이블 생성 + InterviewSession.resumeId + resume_sessions 삭제
│       └── 20260315000001_rls_resumes/migration.sql  # resumes RLS 활성화
├── src/
│   ├── middleware.ts                     # 세션 갱신 + 보호 라우트 리다이렉트 (dashboard/resumes/interview/growth)
│   ├── app/
│   │   ├── layout.tsx                    # RootLayout (Pretendard CDN + globals.css)
│   │   ├── globals.css                   # 디자인 토큰 + glass-card/gradient-text/btn-* 유틸
│   │   ├── (landing)/
│   │   │   └── page.tsx                  # 랜딩 페이지 (로그인 링크 + auth 체크 StartButton)
│   │   ├── (auth)/
│   │   │   ├── layout.tsx                # 인증 전용 레이아웃 (Sidebar 없음, gradient 배경)
│   │   │   ├── login/page.tsx            # 로그인 (이메일/비밀번호 + Google OAuth)
│   │   │   └── signup/page.tsx           # 회원가입 (Zod 검증 + 이메일 확인 안내)
│   │   ├── auth/
│   │   │   └── callback/route.ts         # OAuth 코드 교환 (Open Redirect 방어)
│   │   ├── (app)/
│   │   │   ├── layout.tsx                # Sidebar + main 래퍼 (앱 내부 공통)
│   │   │   ├── resume/page.tsx           # /resumes 리다이렉트 (Server Component)
│   │   │   ├── dashboard/page.tsx        # 대시보드 (최근 면접·이력서·성장 추이 스냅샷)
│   │   │   ├── resumes/
│   │   │   │   ├── page.tsx              # 이력서 목록 + 업로드 인라인 토글 (framer-motion stagger)
│   │   │   │   └── [id]/page.tsx         # 이력서 상세 (원문 + 8축 MOCK + 성장요약 MOCK)
│   │   │   ├── growth/page.tsx           # 성장 추이 (라인 차트 + 바 차트 + 세션 선택)
│   │   │   └── interview/
│   │   │       ├── new/page.tsx          # 면접 시작 페이지 (이력서 선택 → 모드 선택 → 시작)
│   │   │       └── [sessionId]/
│   │   │           ├── page.tsx          # 면접 세션 (채팅 UI + 종료 버튼 + 종료 모달)
│   │   │           └── report/page.tsx   # 8축 역량 평가 리포트 페이지
│   │   └── api/
│   │       ├── resume/questions/route.ts # POST → 엔진 프록시 (pdf-parse 적용, 레거시)
│   │       ├── resumes/
│   │       │   ├── route.ts              # POST → PDF 업로드 + Storage 저장 + Resume DB 생성, GET → per-user 이력서 목록
│   │       │   └── [id]/route.ts         # GET → 이력서 단건 조회 (인증+userId 필터)
│   │       ├── growth/
│   │       │   └── sessions/route.ts     # GET → 완료된 세션 목록 (GrowthSession[])
│   │       ├── interview/
│   │       │   ├── start/route.ts        # POST → 면접 세션 시작
│   │       │   ├── answer/route.ts       # POST → 답변 제출 + 다음 질문
│   │       │   ├── followup/route.ts     # POST → 꼬리질문 생성
│   │       │   └── [sessionId]/
│   │       │       └── complete/route.ts # PATCH → 면접 종료 (sessionComplete=true, 멱등성 보장)
│   │       └── report/
│   │           └── generate/route.ts     # POST → 8축 역량 평가 엔진 프록시 (best-effort saveReport)
│   ├── components/
│   │   ├── Sidebar.tsx                   # 4탭 사이드바 (대시보드/내 이력서/면접/성장 추이, 접기 240px↔68px)
│   │   ├── UploadForm.tsx                # 상태머신 (idle→ready→uploading→done/error)
│   │   ├── QuestionList.tsx              # 카테고리별 질문 + 면접 시작하기 + 다시하기
│   │   ├── InterviewChat.tsx             # 면접 채팅 UI (페르소나별 질문/답변 히스토리)
│   │   └── ReportResult.tsx              # 8축 리포트 (Chart.js Radar + 탭 총평/개선점 + 점수 바)
│   └── lib/
│       ├── types.ts                      # Category, QuestionItem, QuestionsResponse,
│       │                                 # UploadState, PersonaType, FollowupType,
│       │                                 # QueueItem, QuestionWithPersona, HistoryItem,
│       │                                 # InterviewAnswerResponse, AxisScores,
│       │                                 # AxisFeedback, ReportResponse, GrowthSession
│       ├── resume-repository.ts           # Resume Prisma CRUD (create, findById, findDetailById, listByUserId)
│       ├── resume-storage.ts             # Supabase Storage PDF 업로드 헬퍼 (uploadResumePdf)
│       ├── error-messages.ts             # ENGINE_ERROR_MESSAGES + mapDetailToKey
│       ├── auth/
│       │   └── schemas.ts                # Zod: loginSchema, signupSchema, passwordSchema
│       ├── supabase/                     # Supabase 클라이언트
│       │   ├── browser.ts                # createSupabaseBrowser (anon key)
│       │   └── server.ts                 # createServerClient (SSR, cookie) + createServiceClient (service role)
│       └── interview/
│           ├── schemas.ts                # Zod 스키마 (엔진 응답·DB JSON 런타임 검증)
│           ├── interview-repository.ts   # Prisma DB 접근 (complete, saveReport, listCompleted 추가)
│           └── interview-service.ts      # 면접 세션 비즈니스 로직 (engineResultCache 캐싱)
└── tests/
    ├── setup.ts
    ├── api/
    │   ├── error-messages.test.ts
    │   ├── resumes-route.test.ts            # POST/GET /api/resumes + GET /api/resumes/[id] (8케이스)
    │   ├── resume-questions-route.test.ts
    │   ├── interview-answer-route.test.ts
    │   ├── interview-start-route.test.ts
    │   ├── interview-complete-route.test.ts  # PATCH /complete (200/404/멱등성/500)
    │   ├── growth-sessions-route.test.ts     # GET /growth/sessions (목록/빈배열/resumeLabel/500)
    │   └── report-generate-route.test.ts     # 8축 리포트 API 테스트 (200/400/404/422/500)
    ├── unit/
    │   ├── interview-service.test.ts
    │   └── interview-repository.test.ts
    └── ui/
        ├── upload-form.test.tsx
        ├── question-results.test.tsx
        ├── interview-chat.test.tsx
        ├── report-result.test.tsx            # Radar 차트 mock + 탭 전환 테스트 포함
        ├── interview-new-page.test.tsx        # 면접 시작 페이지 (heading, 로딩 상태)
        ├── interview-session-exit.test.tsx    # 종료 버튼 + 모달
        ├── growth-page.test.tsx               # 성장 추이 페이지 (세션 목록, 빈 상태)
        ├── dashboard-page.test.tsx            # 대시보드 (stat cards, 빈 상태)
        └── resumes-detail-page.test.tsx       # 이력서 상세 (원문, 준비 중 badge)
```

## 역할
- 기능 흐름: 자소서 업로드 → 질문 생성 → 면접 시뮬레이션 → 8축 역량 평가 리포트
- engine/ 만 호출 — LLM·파서 직접 구현 금지
- ENGINE_BASE_URL 환경변수로 엔진 접근 (기본값: http://localhost:8000)
- siw가 Prisma/Supabase로 면접 세션 상태 관리 (engine은 stateless)
- Week 1: MVP 완료 (자소서 → 질문 생성 end-to-end 작동)
- Phase 1: 패널 면접 세션 + 꼬리질문 엔진 연동 완료
- Week 4: 특색 기능 1개 이상 + 배포 URL 제출

## 기술 스택
- Next.js 15, React 19, TypeScript (strict)
- Vitest 2, @testing-library/react 16, Playwright (e2e)
- Prisma 7 + @prisma/client + **@prisma/adapter-pg** (v7 드라이버 어댑터 필수)
- **pdf-parse v2** (`PDFParse` 클래스 API, `src/lib/pdf-parser.ts` 추상화)
- `serverExternalPackages: ["pdf-parse"]` — next.config.ts에서 webpack 번들링 제외
- @supabase/ssr, @supabase/supabase-js
- **chart.js ^4 + react-chartjs-2 ^5** — Radar(8축), Line(성장 추이), Bar(축별 비교) 차트
- **framer-motion** — stagger enter 애니메이션 (`staggerChildren: 0.065`, opacity 0→1, translateY 18→0)

## 인증·DB·스토리지
- **인증**: Supabase Auth (Better Auth 미사용)
  - 클라이언트: `@supabase/ssr` createBrowserClient + `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - 서버: createServerClient + `middleware.ts` (`getUser()`로 세션 갱신 필수)
- **DB**: Supabase PostgreSQL + Prisma ORM
  - 런타임: `DATABASE_URL` (pooler, port 6543, `?pgbouncer=true`)
  - 마이그레이션: `DIRECT_URL` (direct, port 5432)
  - Prisma로 테이블 생성 시 RLS 자동 활성화 안됨 — 별도 SQL 마이그레이션 필수
  - Resume 모델: id (uuid), userId, fileName, storageKey, resumeText, questions (Json), createdAt
  - InterviewSession 모델: id (uuid), resumeText, currentQuestion, currentPersona, questionsQueue (Json), history (Json), sessionComplete, engineResultCache (Json?, write-ahead 캐시), **reportScores (Json?, 8축 점수)**, **reportTotalScore (Int?, 총점)**, **resumeId (uuid?, Resume FK)**, createdAt, updatedAt
- **파일 저장**: Supabase Storage (AWS S3 미사용)
  - 버킷명: `SUPABASE_STORAGE_BUCKET` 환경변수 사용 (하드코딩 금지)
  - 서버 경유 업로드 (WAF 보호 유지)
- **보안**: `SUPABASE_SERVICE_ROLE_KEY`는 서버 전용 — `NEXT_PUBLIC_` 접두사 절대 금지
- 환경변수 상세: `docs/specs/mirai/dev_spec.md` §6

## 환경변수 (Phase 1 추가)
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=          # pooler URL (pgbouncer=true)
DIRECT_URL=            # direct URL (마이그레이션용)
ENGINE_BASE_URL=       # 엔진 접근 URL
SUPABASE_STORAGE_BUCKET= # Supabase Storage 버킷명 (하드코딩 금지)
```

## 아키텍처 불변식
- engine은 stateless — 인증·DB 없음
- siw가 Prisma/Supabase로 면접 세션 상태 관리
- engine 수정 없이 siw에서 세션 영속성 구현
- RLS: Supabase에서 interview_sessions, resumes 테이블에 별도 SQL로 활성화 필요

## 작업 전 확인
- `engine/.ai.md` — 엔진 API 계약 확인
- `docs/specs/` — 기능 AC 확인
- 이 파일 (.ai.md) — 현재 구조·진행 상태 확인

## 진행 상태
- [x] Week 1 MVP 01: 자소서 업로드 → 질문 생성 end-to-end (완료)
- [x] Phase 1: 패널 면접 세션 + 꼬리질문 (완료)
  - [x] Prisma InterviewSession 모델 + Supabase 연동
  - [x] interview-repository.ts, interview-service.ts
  - [x] /api/interview/start, /api/interview/answer, /api/interview/followup
  - [x] InterviewChat.tsx 컴포넌트
  - [x] /interview/[sessionId]/page.tsx
  - [x] QuestionList.tsx 업데이트 (면접 시작하기 버튼)
  - [x] pdf-parse 적용 (resume/questions route)
  - [x] 테스트: vitest 30/30, playwright e2e 2/2 통과
  - [x] Prisma v7 드라이버 어댑터(@prisma/adapter-pg) 연동
  - [x] interview-service.ts 엔진 재시도 로직 (LLM JSON 파싱 오류 대응, 3회)
- [x] Issue #63: Pretendard + Glassmorphism 디자인 시스템 적용 (완료)
  - [x] Tailwind v4 + postcss + framer-motion + lucide-react 설치
  - [x] globals.css: shadcn 호환 CSS 변수(:root) + @theme inline (Tailwind v4 색상 매핑)
    - Pretendard @import url() — layout.tsx `<link>` 태그 제거
    - glass-card / glass-card-hover / glass-panel / glass-card-dark
    - gradient-text / btn-primary / btn-outline / tag variants / input-dark
    - layered-card-wrapper / lc-layer-* (3중 레이어 카드 CSS)
    - score-grid-wrapper / axis-row / axis-row__* (8축 점수 그리드 CSS)
    - bg-grid / animations (fadeInUp, card-3d, pulse-ring, shimmer)
  - [x] layout.tsx: Pretendard link 태그 제거 (globals.css @import로 대체), body 단순화
  - [x] 라우트 그룹 재구성: (landing)/ + (app)/
  - [x] (app)/layout.tsx: Sidebar + main 래퍼 (pt-14 md:pt-0 모바일 대응)
  - [x] Sidebar.tsx: 네비게이션 (내 자소서·면접시작·준비중 메뉴, 모바일 햄버거, MirAI 로고 → / 링크)
  - [x] UploadForm.tsx: glass-card + drop zone + btn-primary (TS 타입 수정 포함)
  - [x] QuestionList.tsx: 카테고리 태그 4종 (blue/green/yellow/purple)
  - [x] InterviewChat.tsx: 페르소나별 버블 3종 (hr=blue, tech_lead=green, executive=purple)
  - [x] interview/[sessionId]/page.tsx: 헤더 glass-panel + MirAI 로고 → / 링크 + sessionComplete 완료 카드
  - [x] GET /api/resumes: 목 데이터 반환 (실 DB 연결은 인증 이슈 후 별도 PR)
  - [x] (app)/resumes/page.tsx: 자소서 목록 glass-card + 빈 상태 UI
  - [x] components/landing/LayeredCardWrapper.tsx: 3중 레이어 카드 래퍼 (hover 시 레이어 분리 효과)
  - [x] components/landing/RadarChartInteractive.tsx: 인터랙티브 8축 점수 그리드 (데모 데이터, axis-row hover 인터랙션)
  - [x] (landing)/page.tsx: Nav + Hero(bg-grid + LayeredCardWrapper + RadarChartInteractive) + Features(glass-card-hover) + Personas(glass-card-hover) + CTA(인디고→보라 그라디언트) + Footer
  - [x] 테스트: vitest 32/32 통과, TSC noEmit 에러 없음, next build 성공
- [x] Issue #85: interview 신뢰성 개선 (완료)
  - [x] schemas.ts 신규: Zod 스키마 (QueueItem, HistoryItem, EngineStartResponse, EngineAnswerResponse)
  - [x] interview-repository.ts: findById() Zod parse, saveEngineResult(), updateAfterAnswer() P2025→session_not_found 변환
  - [x] interview-service.ts: engine 응답 Zod parse, engineResultCache write-ahead 캐싱
  - [x] answer/route.ts: session_not_found → 404 분기 추가
  - [x] playwright.config.ts: webServer + CI 환경 분기 (headless, retries, reporter)
  - [x] Prisma: engineResultCache Json? 컬럼 추가 + 마이그레이션 적용
  - [x] 테스트: vitest 57/57, interview-repository.test.ts 신규 (5케이스), tsc --noEmit 에러 0
- [x] Week 3 (Issue #82): 8축 역량 평가 리포트 (완료)
  - [x] AxisScores, AxisFeedback, ReportResponse 타입 추가 (src/lib/types.ts)
  - [x] POST /api/report/generate 엔진 프록시 라우트 (AbortSignal.timeout(90000) + maxDuration=120)
  - [x] ReportResult.tsx 컴포넌트 (score-grid-wrapper/axis-row CSS 재활용)
  - [x] /interview/[sessionId]/report/page.tsx 리포트 페이지 (loading → success/error 상태 머신)
  - [x] 면접 완료 카드에 "리포트 보기" 버튼 추가 (page.tsx TODO 교체)
  - [x] history에서 type 필드 제외 후 엔진 전달 (historyForEngine 패턴)
  - [x] 테스트: report-generate-route.test.ts (API 5케이스) + report-result.test.tsx (UI 5케이스)
  - [x] Prisma 스키마 변경 없음 (기존 resumeText + history 재활용)
- [x] Issue #87: 전체 내비게이션 재설계 (완료)
  - [x] Sidebar.tsx: 4탭 재설계 (대시보드/내 이력서/면접/성장 추이), 접기 버튼 (240px↔68px)
  - [x] /dashboard: 최근 면접·이력서 카드 + 성장 추이 스냅샷 (framer-motion stagger)
  - [x] /resumes: 카드 목록 + 업로드 인라인 토글 (framer-motion stagger)
  - [x] /resumes/[id]: 이력서 상세 (원문 + 8축 MOCK + 성장요약 MOCK, auth #89 후 실데이터)
  - [x] /interview/new: 이력서 선택 → 모드 선택 → 시작 (3-step Client Component)
  - [x] /interview/[sessionId]: 면접 종료 버튼 + 확인 모달 (history 수 분기)
  - [x] /growth: 성장 추이 (Line/Bar chart + 세션 선택 + 강점/약점 분석)
  - [x] PATCH /api/interview/[sessionId]/complete: 면접 종료 (멱등성 보장)
  - [x] GET /api/growth/sessions: 완료 세션 목록 (GrowthSession[], resumeLabel 30자)
  - [x] POST /api/report/generate: best-effort saveReport (성공 후 reportScores 저장)
  - [x] /resume → /resumes 리다이렉트
  - [x] Prisma: reportScores Json?, reportTotalScore Int? 추가 + migration SQL
  - [x] ReportResult.tsx: Chart.js Radar + 탭(총평/개선점) + 점수 바 + grade badge
  - [x] GrowthSession 타입 추가 (types.ts)
  - [x] chart.js, react-chartjs-2 의존성 추가 (package.json)
  - [x] 테스트: 27케이스 신규 (api 8 + ui 19), vitest 전체 통과
- [x] Issue #86: 연습 모드 UI 전체 구현 (완료)
  - [x] InterviewMode, PracticeFeedback 타입 추가 (src/lib/types.ts)
  - [x] Prisma InterviewSession: interviewMode String @default("real") 추가 + migration SQL
  - [x] POST /api/practice/feedback 엔진 프록시 라우트 (AbortSignal.timeout(30000))
  - [x] QuestionList.tsx: "면접 바로 시작" → 실전/연습 모드 선택 카드 2종
  - [x] /interview/new/page.tsx: 이력서 선택 → 모드 선택 → 시작 3-step (모드 sessionStorage 저장)
  - [x] Sidebar.tsx: "면접" 탭 activeCheck 확장 (/interview 전체 커버)
  - [x] InterviewChat.tsx: 연습 모드 피드백 카드 (score·good/improve·keywords·guide·comparisonDelta) + 다시 답변하기/다음 질문으로 버튼
  - [x] /interview/[sessionId]/page.tsx: 연습 모드 흐름 (feedback → retry → nextQuestion), interviewMode sessionStorage 읽기
  - [x] 테스트: practice-feedback-route.test.ts (API 4케이스) + interview-chat.test.tsx 연습 모드 6케이스 추가 (vitest 89/89 통과)
- [x] Issue #88: Supabase Auth 연동 (siw) — end-to-end 인증 흐름 구현
  - [x] src/lib/supabase/server.ts: SSR용 createServerClient 추가 (createServiceClient 유지)
  - [x] src/middleware.ts: 세션 갱신 + 보호 라우트 리다이렉트 (/dashboard, /resumes, /interview, /growth)
  - [x] src/lib/auth/schemas.ts: loginSchema, signupSchema, passwordSchema (Zod)
  - [x] src/app/(auth)/layout.tsx: 인증 전용 레이아웃 (Sidebar 없음, gradient 배경)
  - [x] src/app/(auth)/login/page.tsx: 이메일/비밀번호 + Google OAuth + Open Redirect 방어
  - [x] src/app/(auth)/signup/page.tsx: 이메일/비밀번호 + 이름 + Zod 검증 + 이메일 확인 안내
  - [x] src/app/auth/callback/route.ts: OAuth 코드 교환 (Open Redirect 방어)
  - [x] src/app/(landing)/page.tsx: "로그인" → <Link href="/login">, 시작하기 버튼 → auth 체크 후 라우팅
  - [x] src/components/Sidebar.tsx: 실제 userName/userEmail 표시, 로그아웃 signOut() 연동
  - [x] src/app/(app)/dashboard/page.tsx: "안녕하세요, {이름}님" 실제 이름 표시
  - [x] src/app/api/interview/start/route.ts: getUser() → userId 저장, 미인증 시 401
  - [x] src/app/api/interview/answer/route.ts: auth guard(401) + ownership 체크(403)
  - [x] src/app/api/interview/followup/route.ts: auth guard(401) + ownership 체크(403)
  - [x] src/app/api/interview/[sessionId]/complete/route.ts: auth guard(401) + ownership 체크(403)
  - [x] src/app/api/report/generate/route.ts: auth guard(401) + ownership 체크(403)
  - [x] src/app/api/resumes/route.ts: auth guard(401)
  - [x] src/app/api/growth/sessions/route.ts: 미인증 시 401
  - [x] src/lib/interview/interview-service.ts: start() userId 파라미터 추가
  - [x] src/lib/interview/interview-repository.ts: create() userId 필드 추가, findById() userId 파라미터 추가
  - [x] prisma/migrations/20260315000000_rls_interview_sessions: RLS 활성화 + users_own_sessions 정책
  - [x] Open Redirect `//` 차단 (middleware, login, callback)
  - [x] tests/unit/middleware.test.ts: 미들웨어 리다이렉트·Open Redirect 방어 테스트
  - [x] tests/ui/login.test.tsx: 로그인 폼 유효성·에러 메시지 테스트
  - [x] tests/ui/signup.test.tsx: 회원가입 Zod 정책 테스트 (이름 포함)
- [x] Issue #89: 이력서 영속 저장 — Supabase Storage + Resume Prisma 모델 (완료)
  - [x] Resume Prisma 모델 추가 (id, userId, fileName, storageKey, resumeText, questions Json, createdAt)
  - [x] InterviewSession.resumeId nullable FK 추가
  - [x] ResumeSession 모델 삭제 (Resume으로 대체)
  - [x] src/lib/resume-storage.ts: Supabase Storage PDF 업로드 헬퍼 (uploadResumePdf)
  - [x] src/lib/resume-repository.ts: Resume 기반 전면 재작성 (create, findById, findDetailById, listByUserId)
  - [x] POST /api/resumes: 인증 → PDF 검증 → 엔진 호출 → Storage 업로드 → DB 저장 (maxDuration=60)
  - [x] GET /api/resumes: per-user 이력서 목록 (uploadedAt, questionCount 필드명 유지)
  - [x] GET /api/resumes/[id]: 인증 가드 + userId 필터 (IDOR 방어, Must-Fix 반영)
  - [x] UploadForm.tsx: /api/resume/questions → /api/resumes URL 변경
  - [x] /api/resume/questions/route.ts: resumeRepository 의존성 제거 (레거시 유지)
  - [x] interview-service.ts: findById() 반환 타입 ResumeRecord 대응
  - [x] prisma/migrations/20260315000002_add_resume_model: Resume 테이블 + resumeId FK + resume_sessions 삭제
  - [x] prisma/migrations/20260315000001_rls_resumes: resumes RLS 활성화 (auth.uid()::text 패턴)
  - [x] tests/api/resumes-route.test.ts: POST 200/401/400, GET 200/401, GET[id] 200/401/404 (8케이스)
  - [x] vitest 전체 114/114 통과, tsc --noEmit 에러 0 (prisma.config.ts 기존 이슈 제외)
- [x] Issue #133: Docker 배포 안정화 (완료)
  - [x] entrypoint.sh: `.bin/prisma` symlink → `node node_modules/prisma/build/index.js` 직접 호출
  - [x] Dockerfile: `prisma generate` 명시(builder), `prisma.config.ts` 복사(deps+runner), HEALTHCHECK 추가
  - [x] prisma.config.ts: `import "dotenv/config"` 제거 (Docker top-level dep 아님)
  - [x] next.config.ts: `outputFileTracingIncludes` for pdf-parse 추가
  - [x] deploy-siw.yml: `~/.env.siw` 존재 확인 추가
- [ ] Week 4: 특색 기능 + 배포

## 규칙 변경 시 컨펌 필요
이 파일에 기재된 규칙과 충돌하는 변경을 하려는 경우, 작업을 진행하기 전에 반드시 사람(팀원)에게 먼저 확인을 받아야 한다.
