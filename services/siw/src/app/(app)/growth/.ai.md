# growth — 성장 분석 페이지

## 목적
면접 세션 이력을 기반으로 8축 역량 점수 추이와 강점·개선점 패턴을 시각화한다.

## 파일 구성
- `page.tsx` — 단일 페이지 (use client). Chart.js Line/Bar 차트 + 세션 목록 사용

## 주요 구성
- 세션 목록: `grid-cols-1 md:grid-cols-2` 반응형 그리드
- 강점·약점 패턴: `grid-cols-1 md:grid-cols-2` 반응형 그리드
- 8축 레이블(`AXIS_LABELS`) — 엔진 축 이름과 동기화 필요

## 데이터
- `/api/growth` — `GrowthSession[]` 조회
- `AxisScores` 타입: `communication | problemSolving | logicalThinking | jobExpertise | cultureFit | leadership | creativity | sincerity`

## 반응형
- 모든 다열 grid에 `md:` 브레이크포인트 적용 (#261)
