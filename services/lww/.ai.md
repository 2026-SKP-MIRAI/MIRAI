# services/lww/ — lww 서비스 (카카오톡 채팅 UI 모바일 AI 모의면접)

> 최종 업데이트: 2026-03-22 (이슈 #191 — OAuth 리다이렉트 버그 수정 + confirm 마이그레이션 추가)
> 관련 스펙: `docs/specs/lww/` | 기획서: `docs/whitepaper/lww/proposal.md`

## 목적
카카오톡 채팅 UI 기반 모바일 AI 모의면접 서비스. 취준생이 로그인 없이 바로 면접을 경험하고, 결과 리포트를 받는 "선 체험 후 가입" 구조.

**핵심 원칙**: 재미 1순위. 코인 = 게임머니 (현금화 없음).

## Phase 구조

| Phase | 마감 | 핵심 기능 |
|-------|------|----------|
| **MVP** | 2026-03-22 | 직군 선택 → 채팅 면접(5문항) → 결과 리포트. 로그인 없음. |
| **Phase 1** | 2026-03-27 | 소셜 로그인, 자소서 업로드, 코인 시스템, 게임화 |
| **Phase 2+3** | 2026-04-01 | 커뮤니티, 페르소나 마켓, 쪽지, 채용 매칭 |

## 구현 현황

### MVP (완료, 이슈 #116)
**브랜드 컬러**: Teal #0D9488 (옵션 B 확정)
**빌드**: ✅ 11개 라우트, tsc 0 errors
**E2E 품질**: ✅ Playwright 자동화 10라운드 실행 → 3회 연속 PASS (디자이너/프론트/백엔드/UX 4인 전문가 패널 검토)

### Phase 1 — 소셜 로그인 (이슈 #179, 2026-03-21)
**인증 방식**: 카카오·구글 OAuth + 이메일 가입/로그인 (Supabase Auth)
**아키텍처 원칙**: 익명 우선 — 미들웨어에서 비로그인 사용자 리다이렉트 절대 금지
**익명→인증 마이그레이션**: 로그인 콜백에서 `migrate_anon_to_user` RPC 호출 (원자적 트랜잭션)
**중요**: Tailwind v4에서 `bg-[--color-primary]` 임의값 문법이 포화 색상 변수에 적용 안 됨 → 하드코딩 `bg-[#0D9488]` 사용. `--color-background`, `--color-muted` 등 무채색 변수는 정상 동작.

### UI/UX 확정 사항 (E2E 검증 완료)

- **채팅 레이아웃**: 상단 고정 방식. 첫 번째 아이템으로 시스템 배너("🎙️ AI 면접이 시작됐어요!") 삽입, 메시지는 자연스럽게 아래로 흐름
- **진행 카운터**: `displayIndex = Math.min(questionIndex + 1, total)` — Q1 시작 시 "1/5" 표시
- **면접 완료 카드**: `(state.status === "ending" || isEnding)` 조건으로 렌더링 — `questionIndex >= TOTAL_QUESTIONS`로 끝난 경우도 커버
- **터치 타깃**: 직군 칩 `min-h-[44px]`, 스킵 버튼 `min-h-[44px] min-w-[44px]` (WCAG 2.1 AA)
- **점수 색상**: 낮은 점수 `red` → `amber` (덜 위협적인 UI)
- **텍스트 입력**: `overflow-y-auto` (이전 `overflow-hidden` → 긴 텍스트 잘림 방지)
- **resume 페이지**: 루트 div에 `w-full` 필수 — body `align-items: center` 때문에 회색 거터 방지
- **MobileShell 밖 페이지**: body가 `display:flex; align-items:center; background:#d1d5db` — `(main)`, `(interview)` 그룹 밖 페이지(resume 등)는 반드시 루트 div에 `w-full` 추가

### 라우트 구조
```
/ (main 그룹)          온보딩 슬라이더 → 재방문 시 /onboarding 리다이렉트
/onboarding            직군(최대 3개) + 취준 단계 선택 → 면접 시작
/login                 소셜(카카오·구글) + 이메일 가입/로그인 (Phase 1)
/interview             면접 히스토리 탭 (Server Component — Supabase DB 쿼리)
/interview/[sessionId] 카카오톡 스타일 채팅 면접 UI
/report/[sessionId]    결과 리포트 (점수 + 피드백 + 오브 미리보기)
/resume                자소서 PDF 업로드 → 예상 질문

/auth/callback         GET: OAuth 코드 교환 + 익명 세션 마이그레이션 (Phase 1)
/auth/confirm          GET: 이메일 확인 링크 처리 (token_hash) (Phase 1)

/api/interview/start   POST: 면접 시작 (sessionId 생성, dual-write user_id)
/api/interview/answer  POST: 답변 제출 → 다음 질문 반환
/api/interview/end     POST: 리포트 동기 생성 (maxDuration=110, dual-write user_id)
/api/resume/questions  POST: PDF → 예상 질문
```

### 디렉토리 구조
```
lww/
├── src/
│   ├── app/
│   │   ├── layout.tsx              루트 (Pretendard next/font/local, globals.css)
│   │   ├── globals.css             Tailwind v4 @theme (Teal 팔레트)
│   │   ├── error.tsx               앱 에러 바운더리
│   │   ├── not-found.tsx           404
│   │   ├── middleware.ts            Rate limiting (IP당 10req/min)
│   │   ├── (main)/                 탭 바 있는 화면
│   │   │   ├── layout.tsx          MobileShell + TopBar + BottomTabBar
│   │   │   ├── page.tsx            온보딩 슬라이더
│   │   │   ├── onboarding/         직군 선택
│   │   │   └── interview/          면접 히스토리 탭
│   │   ├── (interview)/            탭 바 없는 면접 화면
│   │   │   ├── layout.tsx          MobileShell only
│   │   │   ├── interview/[id]/     채팅 UI
│   │   │   └── report/[id]/        결과 리포트
│   │   ├── resume/                 자소서 업로드
│   │   └── api/
│   │       ├── interview/          start, answer, end
│   │       └── resume/questions/
│   ├── components/
│   │   ├── ui/                     button, badge, card, dialog, input, progress, separator, toast
│   │   ├── layout/                 MobileShell, TopBar, BottomTabBar
│   │   ├── chat/                   ChatBubble, ChatInput, ChatTopBar, TypingIndicator
│   │   ├── onboarding/             OnboardingSlider, JobCategorySelector
│   │   ├── report/                 LoadingPhaseText, ScoreBar, CategoryScoreGrid, FeedbackSection, OrbPreviewCard
│   │   ├── interview/              InterviewHistoryCard
│   │   └── common/                 EmptyState, NetworkError
│   ├── hooks/
│   │   └── useInterview.ts         면접 상태 훅 (sessionStorage 백업)
│   └── lib/
│       ├── engine-client.ts        engineFetch (allowlist, no-fallback)
│       ├── types.ts                공통 타입 (InterviewState, ReportResponse 등)
│       ├── utils.ts                cn() 헬퍼
│       └── error-messages.ts       엔진 에러 코드 → 한국어 변환
├── tests/                          playwright E2E
├── public/fonts/                   PretendardVariable.woff2
├── components.json                 Shadcn 설정
├── postcss.config.mjs              Tailwind v4 PostCSS
├── package.json
└── playwright.config.ts
```

## 인증·DB·스토리지

### 인증 아키텍처 (Phase 1, 이슈 #179)

**익명 우선 원칙**: LWW는 SIW/Seung과 달리 미들웨어에서 비로그인 사용자를 `/login`으로 리다이렉트하지 않는다.

| 구성 요소 | 역할 |
|-----------|------|
| `middleware.ts` | rate limit → Supabase `getUser()` 세션 갱신 (리다이렉트 없음) |
| `app/auth/callback/route.ts` | OAuth 코드 교환 + `migrate_anon_to_user` RPC |
| `app/auth/confirm/route.ts` | 이메일 확인 링크 처리 (`verifyOtp`) |
| `app/(main)/login/page.tsx` | 소셜(카카오·구글) + 이메일 탭 로그인 UI |
| `components/interview/SaveAccountCTA.tsx` | 비로그인 면접 완료 후 로그인 유도 배너 |

**익명→인증 마이그레이션**:
1. OAuth 로그인 완료 → `/auth/callback` 호출 (또는 이메일 확인 → `/auth/confirm`)
2. `lww_anon_id` 쿠키 읽기
3. `migrate_anon_to_user(p_anon_id, p_user_id)` RPC 실행 (Supabase SQL 트랜잭션)
4. `interview_sessions` + `reports` 양쪽 `user_id` 원자적 업데이트
5. 마이그레이션 성공 후 `lww_anon_id` 쿠키 삭제 (재로그인 시 중복 마이그레이션 방지)
- `confirm/route.ts`에서는 `verifyOtp` 후 `getUser()` 별도 호출로 user ID 획득

**dual-write 패턴** (interview API 라우트):
- `createClient()` + `getUser()` → `userId` 감지
- 로그인이면 `user_id = auth.uid()`, 비로그인이면 `user_id = null`
- `anonymous_id`는 항상 유지 (마이그레이션 완료 후에는 `lww_anon_id` 쿠키 삭제됨)

### DB

- **DB**: Supabase PostgreSQL (스키마: `docs/specs/lww/dev_spec.md` §DB 스키마)
  - 테이블: `interview_sessions`, `reports`, `profiles`
  - RLS 정책: 비로그인은 `anonymous_id` 검증, 로그인은 `auth.uid()` 검증
  - Supabase RPC: `migrate_anon_to_user(p_anon_id text, p_user_id uuid)` — 원자적 마이그레이션
- **스토리지**: Supabase Storage — 버킷명 `SUPABASE_STORAGE_BUCKET` 환경변수 (하드코딩 금지)
- **보안**: `SUPABASE_SERVICE_ROLE_KEY`는 서버 전용 — `NEXT_PUBLIC_` 절대 금지

## 엔진 연동

엔진 API 계약 전문: `engine/.ai.md`

| 기능 | 엔진 엔드포인트 | 서비스 타임아웃 |
|------|----------------|----------------|
| 자소서 → 질문 생성 | `POST /api/resume/questions` | 95s |
| 면접 시작 | `POST /api/interview/start` | 95s |
| 답변 처리 | `POST /api/interview/answer` | 95s |
| 꼬리질문 | `POST /api/interview/followup` | 95s |
| 결과 리포트 | `POST /api/report/generate` | 95s ⚠️ LLM 90s+ 소요 가능 |

**엔진 Stateless 패턴**: 매 호출 시 `history`와 `questionsQueue` 전체를 전달해야 함. 서비스가 DB에 보관 후 전달.

## 환경변수

`.env.example` 참조. 주요 변수:
- `ENGINE_BASE_URL` — 엔진 서비스 URL
- `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` — Supabase 클라이언트
- `SUPABASE_SERVICE_ROLE_KEY` — 서버 전용
- `NEXT_PUBLIC_SITE_URL` — 코드에서 직접 사용되지 않음; Supabase Auth 대시보드 Site URL 설정 참조용
  - OAuth redirect URL은 코드에서 `window.location.origin` 기반 동적 생성 (Vercel Preview 호환)
  - Supabase 대시보드: Authentication → URL Configuration → Site URL + Redirect URLs 설정 필수

## Docker 실행

```bash
docker build -t mirai-lww .
docker run -d -p 3000:3000 --env-file .env mirai-lww
```

## 작업 전 확인

1. `engine/.ai.md` — 엔진 API 계약
2. `docs/specs/lww/dev_spec.md` — 기능 AC, API 명세, DB 스키마
3. `docs/specs/lww/design.md` — 컴포넌트 설계, 인터랙션 정책
4. `docs/specs/lww/storyboard.md` — 화면별 Phase 레이블 확인

## 규칙 변경 시 컨펌 필요
이 파일에 기재된 규칙과 충돌하는 변경을 하려는 경우, 작업을 진행하기 전에 반드시 사람(팀원)에게 먼저 확인을 받아야 한다.
