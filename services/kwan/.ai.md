# services/kwan/ — 김관우

## 목적
김관우의 개인 서비스. 자소서 기반 모의면접 시스템을 end-to-end로 구현.
DDD (Domain-Driven Design) + TDD 적용.

## 현재 진행 상태
- **Week 1 MVP 완료** (2026-03-09): 자소서 PDF 업로드 → 질문 생성 end-to-end 작동
- **Week 2 기능 완료** (2026-03-11): 패널 면접 세션 + 꼬리질문 연동 (engine #40 기반)

## 기술 스택
- **Next.js 16** (App Router, RSC)
- **Prisma 6** + PostgreSQL (Supabase) — DB ORM
- **Vitest** — 단위 테스트 (API routes: node 환경, components: jsdom 환경)
- **Playwright** — e2e 테스트

## 구조
```
kwan/
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx                              메인 페이지 (idle→uploading→done 상태 관리)
│   │   ├── interview/
│   │   │   └── page.tsx                          /interview?sessionId= — 면접 진행 페이지
│   │   └── api/
│   │       ├── resume/questions/route.ts         POST — engine /parse + /questions 병렬 + Prisma 저장
│   │       └── interview/
│   │           ├── start/route.ts                POST { resumeId } → engine start → session 생성
│   │           ├── answer/route.ts               POST { sessionId, answer } → engine answer → session 업데이트
│   │           └── session/route.ts              GET ?sessionId= → session 상태 조회 (새로고침 복원용)
│   ├── domain/interview/
│   │   └── types.ts                              Category, Question, Persona, QuestionWithPersona,
│   │                                             QueueItem, HistoryItem, InterviewSession,
│   │                                             GenerateResult (resumeId 포함), UploadState
│   ├── components/
│   │   ├── UploadForm.tsx                        PDF 선택, "질문 생성" 버튼
│   │   ├── QuestionList.tsx                      카테고리별 질문 그룹핑 + "면접 시작" 버튼 (router.push)
│   │   └── InterviewChat.tsx                     페르소나별 질문 버블 + 꼬리질문 표시 + 답변 입력 + 완료 화면
│   └── lib/
│       ├── engine-client.ts                      callEngineParse / callEngineQuestions / callEngineStart / callEngineAnswer
│       └── db.ts                                 Prisma 클라이언트 싱글톤
├── prisma/
│   └── schema.prisma                             Resume, InterviewSession 모델
├── tests/
│   ├── setup.ts
│   ├── fixtures/input/                           테스트용 JSON fixtures
│   ├── api/
│   │   ├── resume-questions.test.ts              (node env)
│   │   ├── interview-start.test.ts               (node env)
│   │   └── interview-answer.test.ts              (node env)
│   └── components/
│       ├── UploadForm.test.tsx
│       ├── QuestionList.test.tsx
│       └── InterviewChat.test.tsx
├── e2e/
│   └── interview-flow.spec.ts                   Playwright — 업로드→면접→완료 전 과정
├── package.json
├── tsconfig.json
├── vitest.config.ts                              jsdom 환경 (API route 테스트는 파일 단위 node 오버라이드)
├── next.config.ts
├── .env.local                                    실제 값 (커밋 제외)
└── .env.local.example                           환경변수 템플릿
```

## API 라우트

### `POST /api/resume/questions`
- PDF 수신 → engine `/api/resume/parse`로 resumeText 추출
- `Promise.all([engine /api/resume/questions({ resumeText }), Prisma resumes INSERT])` 병렬 실행
- 반환: `{ resumeId, questions, meta }`
- DB 저장 실패 시: `resumeId: null`로 200 반환 (질문은 정상 표시, 면접 시작 불가)
- DB에 `questions: []`로 저장 (병렬 실행으로 실제 질문은 아직 없음, downstream에서 미사용)

### `POST /api/interview/start`
- 입력: `{ resumeId }`
- Prisma에서 resumeText 조회
- engine `POST /api/interview/start { resumeText, personas, mode: 'panel' }` 호출
- Prisma `interview_sessions` 생성 (currentPersonaLabel, currentQuestionType 포함)
- 반환: `{ sessionId, firstQuestion }`

### `POST /api/interview/answer`
- 입력: `{ sessionId, answer }` (answer는 trim 후 최대 5000자)
- Prisma에서 session + resume 조회
- engine `POST /api/interview/answer` 호출 (history, questionsQueue, currentQuestion, currentPersona 전달)
- Prisma session 업데이트 (history에 questionType 포함)
- 반환: `{ nextQuestion, sessionComplete }`

### `GET /api/interview/session`
- 입력: `?sessionId=`
- Prisma session 조회 → history, currentQuestion, sessionComplete 등 반환
- 페이지 새로고침 시 면접 상태 복원 목적

## 환경변수 (`.env.local` 필요 — `.env.local.example` 참고)
```
ENGINE_BASE_URL=http://localhost:8000
DATABASE_URL=postgresql://[user]:[pw]@[host]:6543/postgres?pgbouncer=true&sslmode=require
DIRECT_URL=postgresql://[user]:[pw]@[host]:5432/postgres?sslmode=require
```
주의: `NEXT_PUBLIC_` 접두사 금지 (서버 전용)

## DB 설정
```bash
# 스키마 적용 (개발 환경)
DATABASE_URL="..." DIRECT_URL="..." npx prisma db push

# 타입 생성
npx prisma generate
```
Supabase 콘솔에서 RLS 정책 수동 추가 필요.

## 역할
- 기능 흐름: 자소서 업로드 → 질문 생성 → 면접 시뮬레이션 → (Week 3) 역량 평가
- engine/ 만 호출 — LLM 직접 구현 금지 (HTTP: ENGINE_BASE_URL)
- **Week 1 (완료)**: MVP — 자소서 → 질문 생성 end-to-end
- **Week 2 (완료)**: 패널 면접 세션 + 꼬리질문 연동 + URL 라우팅 + 세션 복원
- Week 3~4: 역량 평가 + 특색 기능 + 배포

## 불변식
```
1. import anthropic, import fitz 금지 — 엔진만 LLM 사용
2. ENGINE_BASE_URL 환경변수로 엔진 통신 (타임아웃 30초)
3. 에러 메시지는 한국어로 사용자 노출
4. 테스트 없는 PR 금지 (npm test 통과 필수)
5. DATABASE_URL / DIRECT_URL — NEXT_PUBLIC_ 접두사 금지
```

## 개발 커맨드
```bash
npm run dev       # localhost:3000
npm test          # Vitest 단위 테스트
npm run build     # 프로덕션 빌드
npx playwright test  # e2e 테스트
```

## 작업 전 확인
- `engine/.ai.md` — 엔진 API 계약 확인
- `docs/specs/mvp/dev_spec.md` — 기능 AC 확인
- 이 파일 (.ai.md) — 현재 구조·진행 상태 확인

## 규칙 변경 시 컨펌 필요
이 파일에 기재된 규칙과 충돌하는 변경을 하려는 경우, 작업을 진행하기 전에 반드시 사람(팀원)에게 먼저 확인을 받아야 한다.
