# services/kwan/ — 김관우

## 목적
김관우의 개인 서비스. 자소서 기반 모의면접 시스템을 end-to-end로 구현.
DDD (Domain-Driven Design) + TDD 적용.

## 현재 진행 상태
- **Week 1 MVP 완료** (2026-03-09): 자소서 PDF 업로드 → 질문 생성 end-to-end 작동
- **Week 2 기능 완료** (2026-03-11): 패널 면접 세션 + 꼬리질문 연동 (engine #40 기반)
- **Phase 3 완료** (2026-03-20): analyze 전환 + PDF Supabase 저장 + 기능 02·05·07 구현
- **프론트엔드 고도화 완료** (2026-03-27): 랜딩 + Supabase Auth + 대시보드 + 디자인 시스템 + 시각화
- **Dark Bento UI 리디자인 완료** (2026-03-27): `globals.css` → Dark Bento 디자인 시스템 전환 (`--kwan-*` CSS 변수), 모든 페이지 다크 테마 적용 (matte-card, score-tile, feedback-card, pill-tab 컴포넌트)

## 기술 스택
- **Next.js 16** (App Router, RSC)
- **Prisma 6** + PostgreSQL (Supabase) — DB ORM
- **Vitest** — 단위 테스트 (API routes: node 환경, components: jsdom 환경)
- **@supabase/supabase-js** — PDF 스토리지 (Service Role Key 사용)
- **@supabase/ssr** — 서버 인증 클라이언트 (ANON_KEY, cookie 기반)
- **recharts** — 대시보드 성장 추이 차트
- **lucide-react** — 아이콘
- **Playwright** — e2e 테스트

## 구조
```
kwan/
├── src/
│   ├── middleware.ts                              인증 미들웨어 (보호 경로 → /login 리다이렉트)
│   ├── app/
│   │   ├── layout.tsx                            async Server Component + auth + NavBar
│   │   ├── page.tsx                              / — Server Component → LandingContent
│   │   ├── LandingContent.tsx                    / — Client Component 랜딩 페이지
│   │   ├── upload/
│   │   │   └── page.tsx                          /upload — 자소서 업로드 (기존 page.tsx 이동)
│   │   ├── dashboard/
│   │   │   └── page.tsx                          /dashboard — 이력서 목록 + 성장 차트
│   │   ├── login/
│   │   │   ├── page.tsx                          /login — Suspense wrapper
│   │   │   ├── LoginForm.tsx                     로그인 폼 (useActionState)
│   │   │   └── actions.ts                        loginAction (Server Action)
│   │   ├── signup/
│   │   │   ├── page.tsx                          /signup — Suspense wrapper
│   │   │   ├── SignupForm.tsx                    회원가입 폼 (useActionState)
│   │   │   └── actions.ts                        signupAction (Server Action)
│   │   ├── auth/callback/
│   │   │   └── route.ts                          인증 콜백 (code → session → /dashboard)
│   │   ├── interview/
│   │   │   └── page.tsx                          /interview — 면접 진행 (matte-card 모드 선택, dark theme)
│   │   ├── diagnosis/
│   │   │   └── page.tsx                          /diagnosis — 5축 진단 (Bento score-tile 그리드)
│   │   ├── report/
│   │   │   └── page.tsx                          /report — 8축 리포트 (ScoreGauge + Bento score-tile 그리드)
│   │   └── api/
│   │       ├── health/
│   │       │   └── route.ts                      GET /api/health — ALB 헬스체크 (인증 제외)
│   │       ├── dashboard/
│   │       │   └── route.ts                      GET — userId 기반 이력서 목록 + 세션/리포트
│   │       ├── user/progress/
│   │       │   └── route.ts                      GET — userId 기반 리포트 점수 히스토리
│   │       ├── resume/
│   │       │   ├── [id]/route.ts                 DELETE — 소유권 확인 + cascade 삭제
│   │       │   ├── questions/route.ts            POST — 인증 + userId/fileName + engine 연동
│   │       │   ├── feedback/route.ts             POST — 인증 + 소유권 검증
│   │       │   └── diagnosis/route.ts            GET — 인증 + 소유권 검증
│   │       ├── practice/
│   │       │   └── feedback/route.ts             POST — 인증만 (stateless)
│   │       ├── report/
│   │       │   ├── generate/route.ts             POST — 인증 + userId + 멱등 생성
│   │       │   └── route.ts                      GET — 인증 + 소유권 검증
│   │       └── interview/
│   │           ├── start/route.ts                POST — 인증 + userId + engine start
│   │           ├── answer/route.ts               POST — 인증 + 소유권 검증
│   │           └── session/route.ts              GET — 인증 + 소유권 검증
│   ├── domain/interview/
│   │   ├── schemas.ts                            Zod 스키마
│   │   └── types.ts                              도메인 타입 + DashboardResumeItem, UserProgressItem 등
│   ├── components/
│   │   ├── NavBar.tsx                            Client — 로그아웃, 랜딩/인증 페이지에서 숨김
│   │   ├── ScoreGauge.tsx                        SVG 원형 게이지 (점수별 색상)
│   │   ├── UploadForm.tsx                        PDF 선택, "질문 생성" 버튼
│   │   ├── QuestionList.tsx                      카테고리별 질문 그룹핑
│   │   └── InterviewChat.tsx                     면접 채팅 + 연습 모드 피드백
│   └── lib/
│       ├── engine-client.ts                      엔진 API 클라이언트
│       ├── supabase.ts                           createServiceClient() — 스토리지 전용 (SERVICE_ROLE_KEY)
│       ├── supabase/server.ts                    createClient() — 인증 전용 (ANON_KEY, cookie 기반)
│       ├── resume-storage.ts                     uploadResumePdf(buffer, fileName) → storageKey
│       └── db.ts                                 Prisma 클라이언트 싱글톤
├── prisma/
│   ├── schema.prisma                             Resume, InterviewSession, Report (userId 포함)
│   └── migrations/
│       ├── 20260324000000_init/                  베이스라인 마이그레이션
│       └── 20260327000000_add_user_fields/       userId, fileName 추가
├── tests/
│   ├── setup.ts
│   ├── fixtures/input/
│   ├── api/
│   │   ├── resume-questions.test.ts
│   │   ├── resume-feedback.test.ts
│   │   ├── resume-diagnosis.test.ts
│   │   ├── practice-feedback.test.ts
│   │   ├── report-generate.test.ts
│   │   ├── report-get.test.ts
│   │   ├── interview-start.test.ts
│   │   ├── interview-answer.test.ts
│   │   ├── interview-session.test.ts
│   │   ├── dashboard.test.ts
│   │   ├── user-progress.test.ts
│   │   └── resume-delete.test.ts
│   └── components/
│       ├── UploadForm.test.tsx
│       ├── QuestionList.test.tsx
│       ├── InterviewChat.test.tsx
│       ├── DiagnosisPage.test.tsx
│       └── ReportPage.test.tsx
├── e2e/
│   └── phase3-full-flow.spec.ts
└── ...
```

## API 라우트

### `POST /api/resume/questions`
- PDF 수신 → buffer 추출 → engine `/api/resume/analyze`로 resumeText + targetRole 추출
- `Promise.all([engine /api/resume/questions({ resumeText, targetRole }), Prisma resumes INSERT])` 병렬
- 반환: `{ resumeId, questions, meta }`
- PDF fire-and-forget 업로드: `uploads/${uuid}.pdf` → storageKey DB 업데이트

### `POST /api/resume/feedback`
- 입력: `{ resumeId, targetRole? }`
- targetRole 폴백: 제공값 → inferredTargetRole → '미지정 직무'
- engine `POST /api/resume/feedback { resumeText, targetRole }` 호출
- diagnosisResult DB 저장 (실패해도 결과 반환)
- 반환: `{ scores (5축), strengths, weaknesses, suggestions }`

### `GET /api/resume/diagnosis`
- 입력: `?resumeId=`
- Prisma resume.diagnosisResult 반환
- diagnosisResult 없으면 404

### `POST /api/practice/feedback`
- 입력: `{ question, answer, previousAnswer? }`
- stateless — DB 없음
- engine `POST /api/practice/feedback` 호출
- 반환: `{ score, feedback, keywords, improvedAnswerGuide, comparisonDelta? }`

### `POST /api/report/generate`
- 입력: `{ sessionId }`
- sessionComplete 확인, history.length >= 5 확인
- 멱등성: 기존 리포트 있으면 200 반환, 없으면 engine 호출 후 DB 저장 → 201
- P2002 동시 요청 시 findUnique fallback → 200
- 반환: `{ reportId }`

### `GET /api/report`
- 입력: `?reportId=`
- 반환: `{ id, sessionId, totalScore, scores (8축), summary, axisFeedbacks, createdAt }`

### `POST /api/interview/start`
- 입력: `{ resumeId, mode? }` — mode: 'practice' | 'real' (기본 'real')
- engine `POST /api/interview/start { resumeText, personas, mode: 'panel' }` 호출
- session.interviewMode 저장

### `POST /api/interview/answer`
- 입력: `{ sessionId, answer }`

### `GET /api/interview/session`
- 입력: `?sessionId=`
- 반환에 `interviewMode` 포함

## DB 스키마 (Prisma)

### Resume
| 필드 | 타입 | 설명 |
|------|------|------|
| id | String (cuid) | PK |
| userId | String? | Supabase Auth user ID |
| fileName | String? | 업로드 파일명 |
| resumeText | String | 추출된 이력서 텍스트 |
| questions | Json | 생성된 질문 목록 |
| storageKey | String? | Supabase 스토리지 경로 |
| inferredTargetRole | String? | analyze 응답에서 추출된 직무 |
| diagnosisResult | Json? | 5축 진단 결과 |
| createdAt | DateTime | |
| @@index([userId]) | | 사용자별 조회 인덱스 |

### InterviewSession
| 필드 | 타입 | 설명 |
|------|------|------|
| userId | String? | Supabase Auth user ID |
| interviewMode | String | 'real' \| 'practice' (기본 'real') |
| report | Report? | 1:1 리포트 관계 |
| (기존 필드) | ... | |

### Report
| 필드 | 타입 | 설명 |
|------|------|------|
| id | String (cuid) | PK |
| userId | String? | Supabase Auth user ID |
| sessionId | String (unique) | InterviewSession FK |
| totalScore | Int | 종합 점수 |
| scores | Json | 8축 점수 |
| summary | String | 종합 평가 |
| axisFeedbacks | Json | 축별 피드백 배열 |
| createdAt | DateTime | |

## 환경변수 (`.env.local` 필요)
```
ENGINE_BASE_URL=http://localhost:8000
DATABASE_URL=postgresql://[user]:[pw]@[host]:6543/postgres?pgbouncer=true&sslmode=require
DIRECT_URL=postgresql://[user]:[pw]@[host]:5432/postgres?sslmode=require
SUPABASE_URL=https://[project].supabase.co
SUPABASE_SERVICE_ROLE_KEY=[service-role-key]
SUPABASE_STORAGE_BUCKET=[bucket-name]
SUPABASE_ANON_KEY=[anon-key]
```
주의: `NEXT_PUBLIC_` 접두사 금지 (서버 전용)

## 타임아웃 계약 (kwan engine-client.ts 기준)
> EC2 standalone 환경에서 `maxDuration`은 강제되지 않음 — engine-client 타임아웃이 실질적 제한.

| 엔드포인트 | engine timeout | route maxDuration | 비고 |
|-----------|---------------|-------------------|------|
| /analyze | 80s | 70s | 이미지 기반 PDF OCR 처리 시간 반영 |
| /questions | 30s | 70s | |
| /interview/start | 40s | 45s | LLM 질문 생성 포함 |
| /interview/answer | 40s | 45s | LLM 꼬리질문 생성 포함 |
| /resume/feedback | 40s | 45s | |
| /practice/feedback | 40s | 45s | |
| /report/generate | 85s | 100s | ALB idle timeout(90s)보다 짧게 설정 |

## 역할
- 기능 흐름: 자소서 업로드 → 질문 생성 → 진단 (기능 02) → 면접 시뮬레이션 → 리포트 (기능 07)
- engine/ 만 호출 — LLM 직접 구현 금지 (HTTP: ENGINE_BASE_URL)
- **Week 1 (완료)**: MVP — 자소서 → 질문 생성 end-to-end
- **Week 2 (완료)**: 패널 면접 세션 + 꼬리질문 연동
- **Phase 3 (완료)**: analyze 전환 + PDF 저장 + 기능 02·05·07 + 연습 모드 UI (모드 선택 + 피드백 패널 + 재답변)
- **프론트엔드 고도화 (완료)**: 랜딩 + Supabase Auth + 대시보드 + MirAI 디자인 시스템 + ScoreGauge
- 테스트: 17개 파일, 150개 TC 전체 PASS

## 불변식
```
1. import anthropic, import fitz 금지 — 엔진만 LLM 사용
2. ENGINE_BASE_URL 환경변수로 엔진 통신
3. 에러 메시지는 한국어로 사용자 노출
4. 테스트 없는 PR 금지 (npm test 통과 필수)
5. DATABASE_URL / DIRECT_URL — NEXT_PUBLIC_ 접두사 금지
6. Supabase Auth 기반 인증, NEXT_PUBLIC_ 미사용 (Server Action + @supabase/ssr)
7. 두 Supabase 클라이언트 공존: supabase.ts (SERVICE_ROLE_KEY, 스토리지) + supabase/server.ts (ANON_KEY, 인증)
8. API 라우트 인증 게이트: health 제외 모든 데이터 API는 인증 필수
9. 소유권 검증: resource.userId && resource.userId !== user.id → 403 (null userId는 레거시 데이터 허용)
```

## 배포 구조 (ECR + EC2 Docker)

- **Dockerfile**: `node:20-alpine` 멀티스테이지 (deps → builder → runner), non-root user, node_modules 전체 복사, CRLF 처리
- **entrypoint.sh**: env guard (DATABASE_URL / DIRECT_URL / ENGINE_BASE_URL) → `prisma migrate deploy` (DIRECT_URL override) → `exec node server.js`
- **deploy-kwan.yml**: `push: main (services/kwan/**)` + `workflow_dispatch (skip_build)` → ECR 빌드·푸시 → EC2 SSH → `--restart unless-stopped`
- **EC2 env-file**: `~/.env.kwan` (DATABASE_URL, DIRECT_URL, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_STORAGE_BUCKET, ENGINE_BASE_URL)
- **NEXT_PUBLIC_*** 없음 — `--build-arg` 불필요
- **헬스체크**: `GET /api/health` → `{ status: 'ok' }`

## 개발 커맨드
```bash
npm run dev       # localhost:3000
npm test          # Vitest 단위 테스트
npm run build     # 프로덕션 빌드
npx playwright test  # e2e 테스트

# Docker 로컬 검증
docker build -t mirai-kwan:test .
docker run --env-file .env.docker -p 3000:3000 mirai-kwan:test
```

## 규칙 변경 시 컨펌 필요
이 파일에 기재된 규칙과 충돌하는 변경을 하려는 경우, 작업을 진행하기 전에 반드시 사람(팀원)에게 먼저 확인을 받아야 한다.
